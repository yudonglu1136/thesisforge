# ThesisForge 上线准备任务清单

更新：2026-09-12。依据：[上线审计](audits/launch-readiness-2026-09-12.md)。

最新约束：**不新增资源预算**。停止独立 staging / RDS / $150新增月预算方案；当前复用现有 AWS 单机、SQLite 用户业务库与 S3。没有新建这些云资源。

最新用户指令：**财报 API 暂缓，其他更新继续**。财报、管理层指引及依赖新财报的模型修订不属于本轮更新，不再作为启动价格/因子/策略工作的前置条件。保留已有真实披露日期；质量因子只从已有来源重算，不能宣称新增财报覆盖。当前市场更新目标为最近已完成交易日 2026-09-11。

当前结论：**NO-GO，尚不能公开上线。** 用户已授权审计、测试、修复并在通过后部署；不再等待重复部署批准，不新增固定成本资源。未通过的关卡不能用放宽数据标准、删除经理、模拟价格、修改截止日期或关闭鉴权替代。最新实际主机/数据库证据见[同步审计](audits/launch-sync-2026-09-12.md)。

状态：待办 / 进行中 / 本机验证通过 / 线上验证通过 / 等待授权 / 外部阻塞。只有“线上验证通过”，才代表对应线上关卡完成。

| 顺序 | 任务 | 状态 | 验收标准 |
|---|---|---|---|
| 01 | 固化审计基线与任务清单 | 本机验证通过 | 保留审计证据、原有脏工作区及真实用户数据；不混淆本机与线上 |
| 02 | API 传输与请求边界 | 本机验证通过；线上待 HTTPS | 生产上游必须 HTTPS；不能通过路径改变上游；内部路由拒绝；请求大小/超时有限制；保持流式响应、压缩和 304 |
| 03 | 依赖漏洞 | 本机验证通过 | qs 6.16.0 与必要的 side-channel 更新；审计 0 漏洞；未升级其他无关依赖 |
| 04 | 用户缓存与并发 | 功能测试通过；完整压测待验收 | 用户隔离；缓存128条/最多64个加载；同用户刷新合并；失效后旧结果不回填；SQLite句柄最多128个 |
| 05 | 静态资源预算 | 本机验证通过 | 42张原头像；144px PNG；像素与元数据未变；1,001,130 bytes < 1 MiB；缓存版本r2 |
| 06 | 用户数据持久化与低成本恢复 | 本机通过；AWS真实16库隔离恢复通过；正式备份未安装 | 复用SQLite；现有私有库结构与本机代码一致；保留目录/身份/密钥；离机持久备份仍待启用 |
| 07 | 现有 AWS 与生产 HTTPS | 现有主机/实际SQL结构已核验；HTTPS待实现 | 不新增服务器或RDS；验证 Vercel→AWS HTTPS、非公开业务库、应用最小权限；不修改现有Ontology域名 |
| 08a | 财报与管理层指引 API 增量 | 用户明确暂缓 | 保留已有财报/指引/模型及其真实日期，不调用未续费API、不标记已更新 |
| 08b | 行情、既有质量因子与策略数据库更新 | 本机已更新；部分源明确保留缺口；AWS未切流 | 9/11：1,479个收益序列、531/533研究报价、指数/CTA；原价格修改0。已有530标的年度质量因子重建；财务与用户数据不变。见[本轮审计](audits/nonfinancial-refresh-2026-09-12.md) |
| 09 | 全 Guru 与策略矩阵 | 策略库32位启用；公开发布门禁未通过 | 158份新增原始披露核验；已知缺失季度明确阻塞。全64个严格/代理门禁62可展示、John Stamas两个失败；还不是当前最终版本的发布验收。全组合矩阵结果分别记录，不以预期阻塞冒充可用 |
| 10 | 多用户日常任务与恢复 | 备份恢复工具已实现；正式调度/告警待办 | 所有已连接用户的同步/NAV 任务；幂等重试；已有S3离机备份、保留策略、告警；实际真实数据恢复并核对 |
| 11 | 全量性能与产品回归 | 本机回归通过；环境级验收待办 | 最新Node22.22.3共1537通过；七组真实worker回测完成；22个PIT节点优化前后输出相同；先前Flutter527、performance47。完整AWS负载基准与多端发布验收仍待办 |
| 12 | 候选发布与最终签收 | 发布保护已测试；提交整理/切流未做 | 包装脚本先验证clean trunk再写zip；前后端/DB版本一致；staging/回滚演练；P0关闭并获授权后切流 |

## 执行记录

- 2026-09-12：开始本机安全修复。生产仍为审计时的 HTTP 上游；**在 HTTPS 资源配置好前，不发布新增的 fail-closed 代理限制**，否则正常 API 会被有意拒绝。
- 代理：生产拒绝HTTP，规范化API路径，保留内部路由404；请求体≤1MiB、接收≤10s、上游全程≤120s；不向客户端返回原始上游异常详情。14项测试通过。
- 用户缓存：同用户60个并发强制刷新合并为一次；连接变更使旧请求不能回填；同步按钮不再清除正在执行的同用户刷新。容量满返回503/Retry-After，而非“券商密钥错误”。
- 用户SQLite：以140个合成用户验证128句柄上限、驱逐后重新读取历史、用户隔离和文件0600权限；不修改现有凭据格式或目录映射。
- 头像：从1,157,636缩小至1,001,130 bytes（13.52%）；逐图验证RGBA像素与所有附加元数据一致。原图备份：`/tmp/tf-guru-png-backup-20260912-launch`。这只是编码压缩，不是换图/降色/降分辨率。
- 发布：新增`npm run test:launch-prep`；打包拒绝未提交/未跟踪文件，当前工作区按预期被拦截，不整体提交其他任务。独立Flutter构建成功，输出`/tmp/tf-launch-build.ssOOWI/site`，未覆盖dist或运行预览。
- 本机验证：Node22.23.2全量服务端1486通过；上线准备专项25通过；Flutter527通过；flutter analyze、i18n通过；Ontology Python 3项 + Node 22项通过；performance47通过；范围内diff检查通过。
- 低成本用户库续项：Node22全量服务端1496通过，用户专项77通过，performance+发布保护合并57通过。新增统一路径、加密WAL备份和隔离恢复；保留production guard、原身份/HMAC/凭据格式。旧自动快照合并hook改为无写入提示，避免部署覆盖新用户修改；未部署到生产。
- 既有AWS S3合成演练通过：2个数据库、24,576 bytes、3个加密对象，9,455ms完成上传/下载/恢复核对，测试对象已清理。使用运维身份，不代表生产实例身份或真实用户迁移已验收；没有新增固定付费资源。
- 续审：实际AWS研究库24表、本机26表，缺质量因子2表/1索引；AWS未配置策略库和研究事件库。现有14组合库+注册/登录2库结构与本机初始化结果一致。16库共581,632 bytes加密隔离恢复通过，最终以应用OS身份执行，335ms，不修改原库；不是正式离机备份。
- 续审：两个域名应用health仍503（旧56曲线中仅1当前可展示）；AWS价格与本机不一致，AVGO仍Q2；财报API复查仍403。增加结构/数据指纹审计、迁移幂等与远端trunk发布保护，25项专项通过，依赖审计0漏洞。没有部署或批量提交脏目录。
- 最终与AWS运行时对齐到Node22.22.3：全服务端及新增审计/发布/主机检查合计1510项通过、0失败。真实用户恢复再次以应用OS身份通过，无异常归属的WAL/SHM侧文件；临时SSH/SSM权限已撤销，EB版本未改变。
- 非财报续项：财报API按最新指令暂停；价格/已有质量因子/策略版本已单独更新并接入本机8789 API。修复英国报价映射、身份版本不一致、缺失季度沿用旧仓风险和因子重复扫描性能问题；1537项测试通过。所有原数据库与用户路径保留；AWS未更新。详见[非财报更新审计](audits/nonfinancial-refresh-2026-09-12.md)。
- 最终策略矩阵793组：481可用、312明确阻塞、0验证异常；不将数据缺口计作已修复。四因子/指数/CTA/混合153组全可用。浏览器已跑出四因子Top10五年四条曲线，历史节点与过滤理由可查看。

详细证据：[本机准备验证](performance/2026-09-12/launch-preparation.md)。
当前实施：[复用现有AWS的用户库/备份/恢复方案](launch-user-data-low-cost-2026-09-12.md)。原托管库方案仅保留为未来扩容参考。
外部数据阻塞：[财报API权限](audits/financial-api-append-2026-09-11.md)、[四位Guru候选源异常](audits/guru-additions-2026-09-12.md)。

## 下一阶段需要的授权/输入

1. 不申请新增预算。实际主机/用户库结构和真实隔离恢复已核验；继续落实已有S3的持久备份、密钥保管和现有资源上的HTTPS。研究库全量完整性仍需在备份候选上验收。
2. 财报API续费按用户最新指令暂缓，不因此暂停其他工作。以后恢复时沿用原数据源/PIT流程；不要在对话中粘贴明文密钥，不换源覆盖历史。
3. 用户已批准测试通过后部署。切流前落实精确迁移对象、冻结窗口与回滚路径；不把本机开发身份/账户记录复制到线上覆盖真实用户。

## 边界

- 现有用户并非完全没有数据库：业务数据分散于每用户 SQLite、登录注册索引和研究事件库；Supabase 目前主要承担认证。这不等于具备可扩展、可恢复的专用用户数据库。
- 不把私人持仓、令牌、账号映射、完整生产导出加入 Git 或公开报告。
- 当前目录有大量其他任务的改动。本轮不整体提交或覆盖；上线前必须按已验证变更集整理候选分支。
- 线上权限/资源阻挡的项目保持未完成；准备脚本与文档不算线上验证通过。

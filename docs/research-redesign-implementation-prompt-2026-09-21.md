# Research 重构实施 Prompt

请在 `/Users/yudonglu/Documents/thesisforge` 中重构 Research，完整遵循 `docs/research-evidence-valuation-redesign-2026-09-21.md` 的产品设计与财务语义。目标是把公司 facts、公告、13F、估值分解、最长十年 DCF、反向 DCF 连成可追溯的研究流程。可以重做页面布局；复用现有 Fact OS、发布模型和有效业务逻辑。

先阅读 AGENTS.md、当前工作区差异和实际接口，保护其他任务的未提交改动。设计文档中的状态基于 2026-09-21 代码，实施前核实已发生的并发变更。直接完成本地实现和验证；发布部署作为独立步骤。不要修改既有历史估值，不要复制数据库或另建平行 facts 存储。

必须实现：

1. 一级导航为研究总览、公告与财务、机构持仓、估值工作台、研究记录。估值工作台内为已发布模型、我的 DCF、反向 DCF。首屏突出本期关键变化、反证、模型依据和下一步问题，价格历史缩小。所有数值可进入来源、期间、币种、计算定义和版本的统一证据侧栏。

2. Research 从公司身份和 facts 出发，不再要求公司一定有已发布估值节点。各能力独立返回覆盖状态。有财务、无模型的公司仍可研究公告、13F、财务和保存观察。复用 `fundamentalResearch.js`，不要重新创造另一套变化计算。

3. 公告提供独立文件时间线及阅读区：SEC/IR 原文、财报、重大事项、指引、电话会、修订和附件关联。复用已有 SEC 元数据解析及有来源的 earnings/guidance。Fact OS events 只有日期和事件代码，必须区分分类线索、已验证原文链接和可引用正文。没有正文不能生成变化摘要。正文按需加载，公开时间与报告期分开，尊重研究截止日。证据只能经用户明确引用进入个人假设，不能自动覆盖平台模型。

4. 机构持仓复用当前 13F v5 的可比样本、拆股归一化、净股数、权重变化和连续轨迹。展示全机构趋势、分页机构列表和机构历季展开，支持 5Y/10Y/全部可用历史。Top10/Top50 明细不能冒充全量样本。保留整份申报缺失与无持仓的区别；Guru 是已验证子集。SF3 季末+45天是代理日期，不能标真实披露时间；持仓变化不能称真实资金流入或推断交易动机。

5. 已发布模型从同一个已选历史节点输出结构化 decomposition。每个有效估值组件必须展示独立值、实际权重、贡献、输入、公式、调整、适用/排除原因和版本。说明类 methodOutputs 不是可加权组件。显示子模型小计、真实存在的后置调整和最终每股价值，完整精度对账后再舍入。历史没有保存的初始权重、置信度和 profile 参数明确留空，不能用当前配置补成历史记录。

6. 以公开 release 中 AMZN 2026-Q2 为验收样本：综合值 $235.6928225956，盈利子模型 100%，FCFE DCF 不可用且权重0；不是 DCF 价值为0。正常化净利润约 $90.59207223bn ÷10.903bn股 ×28.3662662914倍 = $235.692822596。前期是64%盈利/36%DCF；两期模型版本相同但发布公式不同，不把估值上升全部解释为经营改善。更细的来源链见设计文档。

7. 新建版本化通用预测/折现器，个人 DCF 支持 5Y/10Y；同步修改 schema、校验、草稿白名单、模板、保存/恢复、签名、Flutter 控制器、动态表格、终值和所有固定Y5文案。旧五年情景按旧版本可复现；保留 LSEG 的五年定制发布合同，禁止顺手改变历史模型。后五年可向成熟期过渡并逐年覆盖，切换期限不丢输入。

8. 支持有经济依据的早期负现金流及融资/稀释说明。经营驱动模式建立收入→EBIT→现金税→NOPAT→D&A−Capex−ΔNWC→FCFF，以WACC折现并桥接到普通股价值。归母FCFE模式使用Ke，不重复扣净债务。简化FCFE margin不能推导营业利润；要显示净利润必须补利息、税和归属桥。终值采用N+1年可持续现金流、匹配再投资与增长，明确基期和折现时点。按经济类型限制不适用的公司。

9. 反向DCF使用同一正向引擎。一次求一个明确参数：增长路径、成熟利润率或现金兑现参数，固定并展示其他假设。输出逐年隐含收入、利润（仅有桥接时）、FCFF/FCFE、资本投入、终值贡献和价格对账；比较我的情景及有证据的历史。给出增长×利润率的等价格曲线，以呈现多个组合都能解释同一价格。单个价格不能唯一反推所有经营变量；没有一致预期数据不能称分析师共识。保留无解、多解、搜索边界、单调性检查与残差，所有解必须代回正向模型验证。

10. 保存研究问题、支持/反对证据、个人假设、市场隐含情景及失效条件。AI 只做有引用的检索/解释辅助；数值、权重、贴现和求解走确定性代码。不要生成未经支持的买卖评分、机构信心指数或胜率。

验证按实际改动覆盖：AMZN分解对账、旧五年回归、十年独立手算、负显性现金流、终值、正反向round-trip、无解/多解、动态期限保存恢复、缺估值不阻断研究、历史截止日、13F缺失申报与拆股、中英和窄屏、证据跳转及按需加载性能。沿用项目规定的检查，不为简单文案写重复实现的测试。

完成后给出运行方式、关键页面截图、实际通过的检查和仍然缺少的数据覆盖。区分已实现、仅有接口、待接数据，不用虚构样例冒充完成，也不要仅交付静态页面或只增加几张指标卡。
